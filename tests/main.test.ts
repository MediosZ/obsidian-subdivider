// import { toMd, fromMd } from '../src/parser';

test('Should pass', () => {
    expect(1 + 2).toBe(3);
});

// test('Link Escape', () => {
//     const input = `# [Link](https://example.com)`;
//     let md = toMd(fromMd(input))
//     expect(md).toBe(input);
// });

export default {};